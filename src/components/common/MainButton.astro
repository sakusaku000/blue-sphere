---
const {customClass} = Astro.props;
---

<div class={`p-3 px-6 text-gray-50 ring-2 ring-gray-50 rounded-xl hover:bg-gray-50 hover:text-gray-900 duration-200 lg:text-lg 2xl:text-xl cursor-pointer flex items-center ${customClass}`}>
    <slot/>
</div>